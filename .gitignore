# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# Local scratch / artifacts from prior sessions
puppyplogo.png
puppyprep logo.psd
DESIGN_FIELD_JOURNAL.md
PROMPT.md
tokens.json

# Debug screenshots at repo root — local testing artifacts only.
# Keep /assets/images out of this rule — that's product assets.
/fix*.png
/field-*.png
/preview-*.png
/fix-*.png

# Local testing + tool scratch space
.playwright-mcp/
Logo/

# Raw intermediate image generation outputs.
# Final, stripped versions live in assets/images/ and ARE committed.
assets/generated/

# Mobile distribution — NEVER commit signing credentials or service keys
credentials/
*.p12
*.mobileprovision
*.jks
*.keystore
google-play-service-account.json
upload-keystore.jks
GoogleService-Info.plist
google-services.json

# EAS local state
.eas-build-local
